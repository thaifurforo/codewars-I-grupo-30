"""
    verificar arredores
    parameters: posição em tupla (l,c) * com base na função mover
    return: boolean
"""
from mapper import *


verificar_arredores():
    return (c, l)

arredores = verificar_arredores()

mover(arredores):
    mover para(c, l)
    posicao_antiga == "."
    if (c, l) == "S"
    vitória
    elif (c, l) == "."
    remove(c, l) da pilha
    (c, l) = "X"
